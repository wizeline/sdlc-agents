# Remediation Guide

> **Output type:** Remediation Guide
> Generated by the `reviewing-code-for-security` skill.
> Reference this guide alongside the Real-Time Report or Vulnerability Map that surfaced the finding.

---

## Finding Reference

| Field | Value |
|-------|-------|
| **Finding ID** | *(e.g., CR-01, T-006)* |
| **Title** | |
| **Severity** | 🔴 Critical / 🟠 High / 🟡 Medium / 🔵 Low |
| **OWASP Category** | A0X:2025 — [Category] |
| **CWE** | CWE-XXX |
| **ASVS Requirement** | V[X].[Y].[Z] |
| **Reported On** | |
| **Target Fix Date** | |
| **Owner** | |
| **Status** | Open / In Progress / Resolved / Verified |

---

## Vulnerability Overview

### What Is the Problem?

*Explain the vulnerability in plain language. Describe what the code is doing wrong and what security property it violates.*

### Why Does It Matter?

*Describe the real-world impact if exploited. Connect to business risk (data breach, regulatory fine, service disruption, reputational damage).*

### Root Cause

*Identify the underlying cause — missing validation, insecure API usage, misconfigured setting, outdated library, etc.*

---

## Step-by-Step Remediation

### Step 1: Understand the Scope

- [ ] Identify all files / functions where this pattern appears
- [ ] Determine if this is an isolated instance or a systemic issue
- [ ] Check if the vulnerable code path is reachable in production

**How to find all instances:**

```bash
# Example: find all uses of a dangerous function
grep -rn "vulnerable_function(" ./src --include="*.py"
```

---

### Step 2: Apply the Fix

**Before (vulnerable):**

```language
// ❌ Vulnerable pattern
[insert vulnerable code example in user's language and framework]
```

**After (remediated):**

```language
// ✅ Correctly fixed pattern
[insert corrected code example with same variable/function names]
```

**Key changes made:**

1. [Change 1 — what was changed and why]
2. [Change 2]
3. [Change 3]

---

### Step 3: Test the Fix

**Functional test — verify the fix works:**

```language
// Test that the legitimate use case still functions correctly
[test snippet]
```

**Security test — verify the vulnerability is closed:**

```language
// Test that the attack vector is blocked
[security test snippet or manual test instructions]
```

**Expected outcomes:**

| Test | Input | Expected Result |
|------|-------|-----------------|
| Legitimate request | valid input | 200 OK |
| Attack attempt | malicious payload | 400 / 403 / exception safely handled |

---

### Step 4: Prevent Recurrence

| Mechanism | Action |
|-----------|--------|
| **SAST rule** | Add/enable rule `[RULE-ID]` in `[tool]` config |
| **Linting** | Add plugin `[plugin-name]` to catch this pattern |
| **Code review checklist** | Add item: "[ ] No direct concatenation into [X]" |
| **CI gate** | Set gate to fail on `[severity]` findings of type `[type]` |
| **Developer training** | Link to OWASP resource: `[URL]` |

---

### Step 5: Deploy and Verify

- [ ] Code review approved
- [ ] Tests pass in CI (including security tests)
- [ ] SAST re-scan shows finding resolved
- [ ] Deployed to staging and DAST confirms vulnerability is closed
- [ ] Deployed to production
- [ ] Finding marked as **Resolved** in issue tracker

---

## References

| Resource | Link |
|----------|------|
| OWASP Top 10 2025 | |
| CWE Definition | `https://cwe.mitre.org/data/definitions/XXX.html` |
| ASVS Requirement | |
| Language-specific secure coding guide | |
| Internal secure coding standard | |

---

## Verification Sign-Off

| Role | Name | Date | Signature |
|------|------|------|-----------|
| Developer (fix implemented) | | | |
| Reviewer (fix verified) | | | |
| Security (finding closed) | | | |
